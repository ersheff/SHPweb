enum CHMessageType {
    CONTROL = 'control',
    EVENT = 'event',
    MESSAGE = 'message',
    ROOMS = 'room',
    SELF = 'self',
    CHAT = 'chat',
    LIST = 'list',
    FROMSERVER = 'fromserver',
    ERROR = 'error'
}

enum CHChatMode {
    ALL = 'all'
}

enum CHMode {
    PUSH = 'push',
    PUBLISH = 'publish',
}

enum CHTarget {
    ALL = 'all'
}
